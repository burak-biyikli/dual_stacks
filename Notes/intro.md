# High level documents:

`Private Stacks.txt` is my notes for the high level goals of the dual stacks project. 
`README.md` gives the high level structure and relevant tools.

## Lower level documentation
`Memory_Renaming.md` Details the gem5 simulator changes (The exact diff is `patches/gem5.patch`)
`tools/gem5_tests/GEM5_TEST_README.md` Details on the gem5 testing flow

`trace_analyzer_handoff.md` Details the workload analysis part of the pipeline
`tools/trace_analyzer/TRACE_ANALYZER_README.md` Details on the trace analyzer design and its tests

`simulation_pipeline_plan.txt` Details how gem5 runs are done using the trace data

